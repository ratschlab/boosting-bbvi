Setup the deps using conda:
```
conda env create -n <NAME> --file conda-env.txt
```

Then install pip deps:
```
pip install -r requirements.txt
```

This should work:
```
$ python
>>> import edward
```
